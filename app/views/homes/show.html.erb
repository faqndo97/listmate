<!DOCTYPE html>
<html>
  <head>
    <title>ListMate</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  </head>

  <body>
    <div class="flex flex-col min-h-screen">
      <div class="flex-grow">
        <header class="bg-white shadow">
          <div class="container mx-auto px-6 py-3">
            <div class="flex items-center justify-between">
              <div>
                <a href="#" class="text-xl font-semibold text-gray-800">ListMate</a>
              </div>
              <div class="hidden md:flex items-center">
                <nav class="font-semibold text-gray-800">
                  <a href="#" class="mx-4"><%= t('.menu.features') %></a>
                  <a href="#" class="mx-4"><%= t('.menu.pricing') %></a>
                  <a href="#" class="mx-4"><%= t('.menu.about') %></a>
                </nav>
                <%= link_to t('.menu.get_started'), new_list_path, class: "bg-blue-500 text-white px-6 py-2 rounded-md" %>
              </div>
            </div>
          </div>
        </header>

        <main class="container mx-auto px-6 py-12">
          <section class="text-center">
            <h1 class="text-4xl font-bold mb-4"><%= t('.main.heading') %></h1>
            <p class="text-xl text-gray-700"><%= t('.main.subheading') %></p>
            <div class="flex md:hidden justify-center mt-4">
              <%= link_to t('.menu.get_started'), new_list_path, class: "bg-blue-500 text-white px-6 py-2 rounded-md" %>
            </div>
          </section>

          <section class="mt-12 grid md:grid-cols-2 gap-8">
            <div>
              <h2 class="text-2xl font-bold mb-4"><%= t('.main.features.collaborative_lists.title') %></h2>
              <p class="text-gray-700"><%= t('.main.features.collaborative_lists.description') %></p>
            </div>
            <div>
              <h2 class="text-2xl font-bold mb-4"><%= t('.main.features.no_downloads.title') %></h2>
              <p class="text-gray-700"><%= t('.main.features.no_downloads.description') %></p>
            </div>
          </section>

          <section class="mt-12">
            <h2 class="text-2xl font-bold mb-4 text-center"><%= t('.main.pricing.title') %></h2>
            <div class="bg-white p-6 rounded-md shadow">
              <h3 class="text-xl font-bold mb-4"><%= t('.main.pricing.free_plan.title') %></h3>
              <ul class="list-disc pl-8 text-gray-700">
                <li><%= t('.main.pricing.free_plan.feature_1') %></li>
                <li><%= t('.main.pricing.free_plan.feature_2') %></li>
                <li><%= t('.main.pricing.free_plan.feature_3') %></li>
              </ul>
            </div>
          </section>
        </main>
      </div>

      <footer class="bg-white shadow mt-12">
        <div class="container mx-auto px-6 py-3">
          <div class="text-center text-gray-800">
            &copy; <%= Time.current.year %> ListMate. <%= t('.footer.rights_reserved') %>.
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>

